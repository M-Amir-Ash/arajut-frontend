import { Link } from 'react-router-dom'; import ImageWithFallback from './ImageWithFallback'; import { formatRupiah } from '../utils/format'
export default function ProductCard({product}){return <article className="group overflow-hidden rounded-2xl border border-blush/60 bg-white shadow-sm hover:-translate-y-1 hover:shadow-lg"><Link to={`/products/${product.slug}`} className="focus-ring block"><div className="relative aspect-square overflow-hidden bg-cream"><ImageWithFallback src={product.image} alt={product.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/><span className={`absolute left-3 top-3 rounded-full px-3 py-1 text-xs font-bold ${product.readyStock?'bg-white text-[#438064]':'bg-ink text-white'}`}>{product.readyStock?'Ready Stock':'Pre-order'}</span></div><div className="p-4"><p className="text-xs font-semibold uppercase tracking-wider text-primary">{product.category}</p><h3 className="mt-1 line-clamp-2 min-h-12 font-bold text-ink group-hover:text-primary">{product.name}</h3><p className="mt-2 text-lg font-extrabold text-ink">{formatRupiah(product.price)}</p><p className="mt-1 text-xs text-ink/55">Stok {product.stock}</p></div></Link></article>}
