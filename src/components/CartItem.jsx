import ImageWithFallback from './ImageWithFallback'; import { formatRupiah } from '../utils/format'
export default function CartItem({item,onUpdate,onRemove}){return <div className="flex gap-4 border-b border-blush py-5"><ImageWithFallback src={item.image} alt={item.name} className="h-24 w-24 shrink-0 rounded-xl object-cover sm:h-28 sm:w-28"/><div className="min-w-0 flex-1"><h3 className="font-bold">{item.name}</h3><p className="mt-1 text-sm text-primary">{formatRupiah(item.price)}</p><div className="mt-3 flex flex-wrap items-center gap-3"><div className="flex items-center rounded-full border border-blush"><button onClick={()=>onUpdate(item.id,item.quantity-1)} disabled={item.quantity<=1} className="focus-ring h-9 w-9 rounded-full disabled:opacity-30" aria-label="Kurangi jumlah">−</button><span className="w-8 text-center text-sm font-bold">{item.quantity}</span><button onClick={()=>onUpdate(item.id,item.quantity+1)} disabled={item.quantity>=item.stock} className="focus-ring h-9 w-9 rounded-full disabled:opacity-30" aria-label="Tambah jumlah">+</button></div><button onClick={()=>onRemove(item.id)} className="focus-ring rounded text-sm font-semibold text-red-500 hover:text-red-700">Hapus</button></div></div><p className="hidden font-bold sm:block">{formatRupiah(item.price*item.quantity)}</p></div>}
