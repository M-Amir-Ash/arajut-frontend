import {useEffect,useState} from 'react'
import {Link} from 'react-router-dom'
import {apiRequest} from '../config/api'
import {formatRupiah} from '../utils/format'
export default function Orders(){const[orders,setOrders]=useState([]);const[error,setError]=useState('');useEffect(()=>{apiRequest('/orders').then(r=>setOrders(r.data||[])).catch(e=>setError(e.message))},[]);return <main className="container-page py-12"><h1 className="font-serif text-4xl font-bold">Pesanan saya</h1>{error&&<p className="mt-5 rounded-xl bg-red-50 p-3 text-red-700">{error}</p>}<div className="mt-7 space-y-4">{orders.map(o=><article key={o.id} className="rounded-2xl bg-white p-6 shadow-sm"><div className="flex flex-wrap justify-between gap-3"><div><b>{o.order_number}</b><p className="text-sm text-ink/55">{o.shipment?.courier} · {o.shipment?.service}</p></div><span className="rounded-full bg-blush px-3 py-1 text-sm font-bold">{o.status}</span></div><div className="mt-4 flex flex-wrap justify-between gap-3"><span>Total: <b>{formatRupiah(o.total)}</b></span><span>Sisa bayar: <b className="text-primary">{formatRupiah(o.remaining_payment)}</b></span></div>{o.remaining_payment>0&&<Link to={`/payment/${o.id}`} className="mt-4 inline-block rounded-full bg-primary px-5 py-2 font-bold text-white">Bayar sekarang</Link>}</article>)}{!orders.length&&!error&&<p className="rounded-2xl bg-white p-10 text-center text-ink/60">Belum ada pesanan.</p>}</div></main>}
