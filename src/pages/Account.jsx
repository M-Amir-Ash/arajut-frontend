import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';import FormField from '../components/FormField';import EmptyState from '../components/EmptyState'
export default function Account(){const {user,updateProfile,logout}=useAuth();const [f,setF]=useState(user);const [saved,setSaved]=useState(false);const nav=useNavigate();return <main className="container-page py-12"><h1 className="font-serif text-4xl font-bold">Halo, {user.name.split(' ')[0]}!</h1><div className="mt-8 grid gap-8 lg:grid-cols-2"><form onSubmit={e=>{e.preventDefault();updateProfile(f);setSaved(true)}} className="rounded-2xl border border-blush bg-white p-6"><h2 className="text-xl font-bold">Profil saya</h2>{saved&&<p className="mt-3 rounded-lg bg-green-50 p-3 text-sm text-green-700">Profil berhasil diperbarui.</p>}<div className="mt-5 space-y-4"><FormField label="Nama lengkap" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><FormField label="Email" type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><FormField label="Nomor telepon" value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></div><div className="mt-6 flex gap-3"><button className="rounded-full bg-primary px-5 py-2.5 font-bold text-white">Simpan</button><button type="button" onClick={()=>{logout();nav('/')}} className="rounded-full border border-ink/15 px-5 py-2.5 font-bold">Keluar</button></div></form><section><h2 className="mb-5 text-xl font-bold">Riwayat transaksi</h2><EmptyState icon="📦" title="Belum ada pesanan" text="Pesanan yang sudah kamu buat akan tampil di sini."/></section></div></main>}
