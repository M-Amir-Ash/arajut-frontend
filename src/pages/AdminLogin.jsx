import{useState}from'react';import{useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';import FormField from'../components/FormField'
export default function AdminLogin(){const[f,setF]=useState({email:'',password:''});const[error,setError]=useState('');const{adminLogin,logout}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();setError('');try{const user=await adminLogin(f.email,f.password,false);if(user.role!=='admin'){await logout();throw new Error('Akun ini tidak memiliki akses admin.')}nav('/admin')}catch(err){setError(err.message)}};return <main className="min-h-screen bg-cream px-4 py-16"><form onSubmit={submit} className="mx-auto max-w-md rounded-3xl bg-white p-8 shadow-lg"><p className="text-sm font-bold text-primary">ARAJUT</p><h1 className="mt-2 font-serif text-3xl font-bold">Login Admin</h1>{error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="mt-6 space-y-5"><FormField label="Email admin" type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><FormField label="Kata sandi" type="password" required value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></div><button className="mt-7 w-full rounded-full bg-primary py-3 font-bold text-white">Masuk ke Dashboard</button></form></main>}
