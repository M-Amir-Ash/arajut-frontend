import {useState} from 'react';import {Link,useLocation,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';import FormField from '../components/FormField'
export default function Login(){const [form,setForm]=useState({email:'',password:'',remember:false});const [error,setError]=useState('');const [show,setShow]=useState(false);const {login}=useAuth();const nav=useNavigate();const loc=useLocation();const submit=e=>{e.preventDefault();try{login(form.email,form.password,form.remember);nav(loc.state?.from||'/account',{replace:true})}catch(err){setError(err.message)}};return <main className="container-page py-14"><form onSubmit={submit} className="mx-auto max-w-md rounded-3xl border border-blush bg-white p-7 shadow-lg"><h1 className="font-serif text-3xl font-bold">Selamat datang kembali</h1><p className="mt-2 text-sm text-ink/60">Masuk untuk melihat akun Arajut kamu.</p>{error&&<p className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="mt-6 space-y-5"><FormField label="Email" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><div className="relative"><FormField label="Kata sandi" type={show?'text':'password'} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-9 p-2 text-xs text-primary">{show?'Sembunyikan':'Lihat'}</button></div><div className="flex justify-between text-sm"><label><input type="checkbox" checked={form.remember} onChange={e=>setForm({...form,remember:e.target.checked})}/> Ingat saya</label><span title="Akan hadir di fase berikutnya" className="text-primary">Lupa kata sandi?</span></div></div><button className="mt-7 w-full rounded-full bg-primary py-3 font-bold text-white">Masuk</button><p className="mt-5 text-center text-sm">Belum punya akun? <Link to="/signup" className="font-bold text-primary">Daftar</Link></p></form></main>}
