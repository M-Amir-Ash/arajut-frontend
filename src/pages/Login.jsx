import{useState}from'react';import{Link,useLocation,useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';import FormField from'../components/FormField'
export default function Login(){const[f,setF]=useState({email:'',password:'',remember:false});const[error,setError]=useState('');const[show,setShow]=useState(false);const{login,loading}=useAuth();const nav=useNavigate(),loc=useLocation();const submit=async e=>{e.preventDefault();setError('');try{await login(f.email,f.password,f.remember);nav(loc.state?.from||'/account',{replace:true})}catch(err){setError(err.message)}};return <main className="container-page py-14"><form onSubmit={submit} className="mx-auto max-w-md rounded-3xl border border-blush bg-white p-7 shadow-lg"><h1 className="font-serif text-3xl font-bold">Selamat datang kembali</h1>{error&&<p className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="mt-6 space-y-5"><FormField label="Email" type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><div className="relative"><FormField label="Kata sandi" type={show?'text':'password'} required value={f.password} onChange={e=>setF({...f,password:e.target.value})}/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-9 p-2 text-xs text-primary">{show?'Sembunyikan':'Lihat'}</button></div><label className="text-sm"><input type="checkbox" checked={f.remember} onChange={e=>setF({...f,remember:e.target.checked})}/> Ingat saya</label></div><button disabled={loading} className="mt-7 w-full rounded-full bg-primary py-3 font-bold text-white disabled:opacity-50">{loading?'Memproses...':'Masuk'}</button><p className="mt-5 text-center text-sm">Belum punya akun? <Link to="/signup" className="font-bold text-primary">Daftar</Link></p></form></main>}
