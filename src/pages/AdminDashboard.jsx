import {Link} from 'react-router-dom';import {useData} from '../context/DataContext';import {formatRupiah} from '../utils/format';import ImageWithFallback from '../components/ImageWithFallback'
export default function AdminDashboard(){const {products,categories}=useData();const stats=[['Total produk',products.length],['Ready stock',products.filter(p=>p.readyStock).length],['Pre-order',products.filter(p=>!p.readyStock).length],['Stok rendah',products.filter(p=>p.stock<5).length],['Kategori',categories.length],['Unggulan',products.filter(p=>p.featured).length]];return <><h1 className="font-serif text-3xl font-bold">Dashboard</h1><div className="mt-6 grid grid-cols-2 gap-4 lg:grid-cols-3 xl:grid-cols-6">{stats.map(([x,n])=><div key={x} className="rounded-2xl bg-white p-5 shadow-sm"><p className="text-sm text-ink/55">{x}</p><p className="mt-2 text-3xl font-bold text-primary">{n}</p></div>)}</div><section className="mt-8 rounded-2xl bg-white p-5"><div className="flex justify-between"><h2 className="text-xl font-bold">Produk terbaru</h2><Link to="/admin/products" className="text-sm font-bold text-primary">Kelola semua</Link></div><div className="mt-4 overflow-x-auto"><table className="w-full min-w-2xl text-left text-sm"><thead className="border-b border-blush text-ink/50"><tr>{['Produk','Kategori','Harga','Stok','Status',''].map(x=><th key={x} className="p-3">{x}</th>)}</tr></thead><tbody>{products.slice(-6).reverse().map(p=><tr key={p.id} className="border-b border-ink/5"><td className="p-3"><div className="flex items-center gap-3"><ImageWithFallback src={p.image} alt="" className="h-10 w-10 rounded-lg object-cover"/><b>{p.name}</b></div></td><td>{p.category}</td><td>{formatRupiah(p.price)}</td><td>{p.stock}</td><td>{p.readyStock?'Ready':'Pre-order'}</td><td><Link to={`/admin/products/${p.id}/edit`} className="text-primary">Edit</Link></td></tr>)}</tbody></table></div></section></>}
